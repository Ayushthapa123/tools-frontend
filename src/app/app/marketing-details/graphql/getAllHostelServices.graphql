query GetAllHostelServices{
  getAllHostelServices{
    data {
      id
      title
      description
      hostelServiceType
      priority
      status
      budget
      dueDate
      completionDate
      createdAt
      hostel {
        name
      }
    }
    error {
      message
    }
  }
}