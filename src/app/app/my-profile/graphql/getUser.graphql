query getUserById($id: Float!) {
  getUserById(id: $id) {
    data {
    id
    isVerified
    fullName
    email
    phoneNumber
    altPhoneNumber
    city
    gender
    dateOfBirth
    profilePicture
    homestayId
  }
  error {
    message
    code
  }
} 
}
