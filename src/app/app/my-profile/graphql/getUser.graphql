query getUserById($id: Float!) {
  getUserById(id: $id) {
    data {
      id
      isVerified
      fullName
      email
      phoneNumber
      altPhoneNumber
      
      gender
      dateOfBirth
      profilePicture
      
    }
    error {
      message
      code
    }
  }
}
