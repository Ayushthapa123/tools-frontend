mutation updateUser($input: UpdateUserInput!) {
  updateUser(input: $input) {
    data {
      id
      fullName
      email
      phoneNumber
      altPhoneNumber
      
      gender
      dateOfBirth
      profilePicture
    }
    error {
      message
      code
    }
  }
}
