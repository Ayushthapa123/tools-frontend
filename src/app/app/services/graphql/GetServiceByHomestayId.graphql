query GetServiceByHomestayId($homestayId: Float!) {
  findServiceByHomestayId(homestayId: $homestayId) {
    data {
      id
      service
      homestayId
      createdAt
      updatedAt
    }
    error {
      message
      code
    }
  }
}