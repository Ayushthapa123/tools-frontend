query GetServiceByHostelId($hostelId: Float!) {
  findServiceByHostelId(hostelId: $hostelId) {
    data {
      id
      services
      hostelId
    }
    error {
      message
      code
    }
  }
}
