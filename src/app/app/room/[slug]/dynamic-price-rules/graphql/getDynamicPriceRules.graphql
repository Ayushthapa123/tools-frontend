query GetPriceRulesByRoom($roomId: Int!) {
priceRulesByRoom(roomId: $roomId) {
    data {
    id
    name
    description
    amount
    startDate
    endDate
    isWeekend
    isActive
    priority
    }
    

  
  }
}