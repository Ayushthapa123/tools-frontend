query GetPriceRulesByRoom($roomId: Int!) {
priceRulesByRoom(roomId: $roomId) {
    id
    name
    description
    roomId
    startDate
    endDate
    amount
    isWeekend
    isActive
    priority
    
  }
}