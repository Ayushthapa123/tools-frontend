query GetRoomImagesByRoomId($roomId: Int!) {
  getRoomImagesByRoomId(roomId: $roomId) {
    data {
    id
    roomId
    url
    caption
    createdAt
    updatedAt
  }
  error {
    message
      code
    }
  }
}
