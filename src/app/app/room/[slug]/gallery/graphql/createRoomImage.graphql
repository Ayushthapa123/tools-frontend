mutation CreateRoomImage($data: CreateRoomImageInput!) {
  createRoomImage(data: $data) {
    data {
      id
      roomId
      url
      caption
    }
    error {
      message
      code
    }
  }
}
