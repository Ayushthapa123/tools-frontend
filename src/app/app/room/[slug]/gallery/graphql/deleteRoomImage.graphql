mutation DeleteRoomImage($roomImageId: Int!) {
  deleteRoomImage(roomImageId: $roomImageId) {
    data {
    id
    roomId
    url
    caption
    createdAt
    updatedAt
    }
    error {
      message
      code
    }
  }
}