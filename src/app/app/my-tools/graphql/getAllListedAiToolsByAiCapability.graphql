query GetListedAiToolsByAiCapability(
  $pageNumber: Int = 1
  $pageSize: Int = 6
  $aiCapability: AiCapability!
) {
  getListedAiToolsByAiCapability(
    pageNumber: $pageNumber
    pageSize: $pageSize
    aiCapability: $aiCapability
  ) {
    data {
      id
      name
      slug
      shortDescription
      logoUrl
      websiteUrl
      videoUrl
      pricingType
      aiType 
      domains 
      modalities 
      
      toolUserTypes
      platforms
      integrationOptions
      popularityScore
      featured
      verified
      useCases
      createdAt
      updatedAt
    }
    error {
      message
      code
      path
    }
  }
}