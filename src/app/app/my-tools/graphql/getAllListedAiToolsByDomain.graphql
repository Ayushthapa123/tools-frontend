query GetListedAiToolsByDomain(
  $pageNumber: Int = 1
  $pageSize: Int = 6
  $domain: Domain!
) {
  getListedAiToolsByDomain(
    pageNumber: $pageNumber
    pageSize: $pageSize
    domain: $domain
  ) {
    data {
      id
      name
      slug
      shortDescription
      logoUrl
      websiteUrl
      videoUrl
      pricingType
      aiType 
      domains 
      modalities 
      
      toolUserTypes
      platforms
      integrationOptions
      popularityScore
      featured
      verified
      useCases
      createdAt
      updatedAt
    }
    error {
      message
      code
      path
    }
  }
}