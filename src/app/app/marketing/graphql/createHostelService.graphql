mutation createHostelService($data:CreateHostelServiceDto!){
  createHostelService(input:$data){
    data{
      budget
      description
      id
      title
    }
    error{
      message
    }
  }
}