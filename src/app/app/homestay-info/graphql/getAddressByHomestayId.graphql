query getAddressByHomestayId($homestayId: Float!) {
 getAddressByHomestayId(homestayId: $homestayId) {
   data {
   id
   country    
   city
   subCity
   street
   latitude
   longitude
 }
 error {
  message
  code
 }
}
}
