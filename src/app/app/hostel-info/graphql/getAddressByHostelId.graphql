query getAddressByHostelId($hostelId: Float!) {
  getAddressByHostelId(hostelId: $hostelId) {
    data {
      id
      country
      city
      subCity
      street
      latitude
      longitude
    }
    error {
      message
      code
    }
  }
}
