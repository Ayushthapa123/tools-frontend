query getHostelByToken {
  getHostelByToken {
    data {
      id
      name
      description
      slug
      verifiedBySuperAdmin
      verifiedByCommunityOwner
      hasOnboardingComplete
    }
    error {
      message
      code
    }
  }
}
