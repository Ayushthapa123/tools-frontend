mutation createOnboardingHostel($input: CreateOnboardingHostelInput!) {
  createOnboardingHostel(data: $input) {
    data {
      id
      ownerId
    }
    error {
      message
      code
    }
  }
}
