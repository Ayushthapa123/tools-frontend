query FindAmenityByHostelId($hostelId: Int!) {
  findAmenityByHostelId(hostelId: $hostelId) {
    data {
      id
      amenities
      hostelId
    }
    error {
      message
      code
    }
  }
}
