query GetFeaturedHomestays($pageSize: Int!) {
  getAllHomestays(pageSize: $pageSize) {
    data {
    id
    name
    description
    slug
    address {
      country
      city
      subCity
      street
      latitude
      longitude
    }
    image {
      url
      caption
      isSelected
    }
    rooms {
      id
      caption 
      capacity 
      roomNumber
      status
      createdAt
      updatedAt
      homestayId
      image {
        url 
        id 
        caption
        createdAt
        updatedAt
        roomId
      }
      price {
        baseAmount 
        currency
        isDynamicPricing
    
        discountAmount
        discountType
      }
    }
    services {
      service
    }
  }
  error {
    message
    code
  }
}
}